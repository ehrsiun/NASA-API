import { defineConfig } from "vite";

export default defineConfig({
  // IMPORTANT: Replace YOUR_REPO_NAME_HERE with your GitHub repo name, including trailing slash.
  // Example: repo "nasa-eonet-map" -> base: "/nasa-eonet-map/"
  base: "/NASA-API/"
});
